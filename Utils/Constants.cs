namespace schoolApp.Utils;

internal class Constants
{
    private const decimal averange = 7;
    private const decimal salary = 1400;

    public decimal GetAverange { get => averange; }
    public decimal GetSalary { get => salary; }
}
